# config/packages/nelmio_api_doc.yaml

nelmio_api_doc:
    documentation:
        info:
            title: My App
            description: This is an awesome app!
            version: 1.0.0
        components:
            schemas:
                Product:
                    type: object
                    properties:
                        id:
                            type: integer
                        name:
                            type: string
                        quantity:
                            type: integer
                    required:
                        - id
                        - name
                        - quantity

    # 🔑 Ключевая настройка: фильтрация маршрутов
    areas:
        default:
            path_patterns:
                - ^/api/  # Включать ТОЛЬКО маршруты, начинающиеся с /api/
            # Опционально: явно исключить (обычно не нужно, если path_patterns достаточно)
            # excluded_paths:
            #   - ^/_error/
            #   - ^/_wdt/
            #   - ^/_profiler/
