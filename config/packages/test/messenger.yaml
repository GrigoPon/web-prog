# config/packages/test/messenger.yaml
framework:
    messenger:
        transports:
            # Переопределяем ВСЕ транспорты из основного файла
            user_events: 'in-memory://'
            product_events: 'in-memory://'
